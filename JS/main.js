//* Esto va a servir para actualizar la hora cada vez que se ingrese a la pagina 

var tiempo = new Date();
console.log(time.getHours() + ":" + time.getMinutes() + ":" + time.getSeconds());

///* esta funcion nos va a servir para que nuestro carrusel tenga slider(desplazamiento)
function carousel(container) {
    const slider = container.querySelector('.slider');
    const items = slider.querySelectorAll('.item');
  }